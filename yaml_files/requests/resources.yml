apiVersion: v1
kind: Pod
metadata:
  name: resources-pod
spec:
  containers:
  - name: apache-container
    image: httpd:latest

    resources:
      requests:
        memory: "128Mi"
        cpu:    "500m"