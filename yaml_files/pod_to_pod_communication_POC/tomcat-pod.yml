apiVersion: v1
kind: Pod

metadata:
  name: tomcat-pod
  labels:
    apps: app-python

spec:
  containers:
    - name: container-tomcat
      image: tomcat:latest
      ports:
        - containerPort: 8080